{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start:dev":"concurrently \"npm run server\" \"npm run client-watch\"",
    "server": "cd server && npm start",
    "client": "cd client && npm run build",
    "client-watch": "cd client && npm run start",
    "install": "npm i --prefix client && npm i --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}